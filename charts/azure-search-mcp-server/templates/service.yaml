apiVersion: v1
kind: Service
metadata:
  name: {{ include "azure-search-mcp-server.fullname" . }}
  labels:
    {{- include "azure-search-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "azure-search-mcp-server.selectorLabels" . | nindent 4 }}
