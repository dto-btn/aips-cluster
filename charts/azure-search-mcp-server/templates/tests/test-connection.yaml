apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azure-search-mcp-server.fullname" . }}-test-connection"
  labels:
    {{- include "azure-search-mcp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azure-search-mcp-server.fullname" . }}:{{ .Values.service.port }}/mcp']
  restartPolicy: Never
