apiVersion: v1
kind: Service
metadata:
  name: {{ include "geds-mcp-server.fullname" . }}
  labels:
    {{- include "geds-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "geds-mcp-server.selectorLabels" . | nindent 4 }}
