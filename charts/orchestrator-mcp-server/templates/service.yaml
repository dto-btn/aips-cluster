apiVersion: v1
kind: Service
metadata:
  name: {{ include "orchestrator-mcp-server.fullname" . }}
  labels:
    {{- include "orchestrator-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "orchestrator-mcp-server.selectorLabels" . | nindent 4 }}
