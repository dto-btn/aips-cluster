apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bits-mcp-server.fullname" . }}-test-connection"
  labels:
    {{- include "bits-mcp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bits-mcp-server.fullname" . }}:{{ .Values.service.port }}/mcp']
  restartPolicy: Never
