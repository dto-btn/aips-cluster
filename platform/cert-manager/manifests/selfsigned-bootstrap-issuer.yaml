# Bootstrap-only issuer used exclusively to self-sign the cluster's internal
# root CA certificate (see root-ca-certificate.yaml). Nothing else should
# reference this issuer.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-bootstrap
spec:
  selfSigned: {}
